namespace WikiExtractor.Maui.App.Controls
{
    // Marker subclass — kept so existing XAML compiles without changes.
    // All swipe logic now lives in SwipeCollectionViewHandler (ItemTouchHelper).
    public class HorizontalSwipeBorder : Border { }
}
